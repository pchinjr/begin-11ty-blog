{
  "name": "begin-eleventy-blog",
  "version": "0.0.2",
  "scripts": {
    "build": "eleventy",
    "watch": "eleventy --serve",
    "start": "npm run watch & ARC_STATIC_SPA=false sandbox"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@11ty/eleventy": "^0.10.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.1",
    "@architect/sandbox": "^1.8.3"
  }
}
